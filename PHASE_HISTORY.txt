PHASE_15D.txt
---------------
Phase 15d complete: structured alert filter activation.

Implemented:
- activated structured `--category` and `--entity-kind/--entity-value` filters for alert list/search/export
- activated `alert_idx_cat` candidate selection when category-index coverage is complete enough to preserve correctness
- activated `alert_idx_ent` candidate selection when the specific entity filter matches the primary alert set exactly
- kept mixed-history and older tenants correct by falling back to primary alert scans when structured-filter index coverage is absent or incomplete

Next:
- Phase 16a: replay cadence and spool-cap tuning


PHASE_15C.txt
---------------
Phase 15c complete: secondary alert index query activation.

Implemented:
- activated the persisted `alert_idx_time` path for list/search/export candidate selection when tenant time-index coverage matches the primary alert set
- kept mixed-history and older tenants correct by falling back to primary alert scans when the time index is absent or incomplete
- preserved deterministic ordering and the primary `AlertV1` drill/export model while adding tenant DB and alerts-query coverage for the index-aware path

Next:
- Phase 15d: structured alert filter activation


PHASE_14B.txt
---------------
Phase 14b complete: recovery visibility and tuning.

Implemented:
- added recovery backlog visibility to `status`, `/metrics`, and `/healthz`
- activated carefully scoped output config tuning for deterministic automated replay max-files-per-pass
- kept replay cadence and spool-cap tuning deferred so the active runtime/config surface stays narrow
- added status/config/run coverage for the recovery visibility and tuning surface

Next:
- Phase 15a: scoring policy activation


PHASE HISTORY
=============

This file rolls up the historical `PHASE*.txt` checkpoint notes that were previously stored
as separate files at the repository root. The original files were consolidated here during
the post-Phase-12 closeout so future review can happen from a single history file.

Ordering follows the original checkpoint sequence as closely as possible.

PHASE_14A.txt
---------------
Phase 14a complete: output recovery automation.

Implemented:
- activated automatic jsonl-failure-to-spool fallback for the active `run` and `oneshot` jsonl sink path
- activated bounded deterministic automated replay passes for `run` and `oneshot` while keeping `replay-spool` as the full-drain manual operator command
- kept spool tuning on internal defaults so the recovery contract could expand without changing the active config surface mid-phase
- added helper and integration coverage for bounded replay plus automated `run`/`oneshot` replay behavior

Next:
- Phase 14b: recovery visibility and tuning


PHASE_13B.txt
---------------
Phase 13b complete: release hardening and final operator ergonomics.

Implemented:
- added derived observability endpoint URLs to the shared status snapshot and both text/JSON status output
- hardened observability startup so a later bind failure shuts down any earlier endpoint listener started during the same run attempt
- added deterministic endpoint behavior coverage for wrong-path 404 and non-GET 405 handling
- reconciled remaining observability contract/readme/doc drift after Phase 13a

Next:
- Phase 14a: output recovery automation

PHASE_12_5G.txt
---------------
Phase 12.5g complete: final consistency sweep and closeout.

Implemented:
- completed a final consistency sweep across contracts, docs, config wording, and tests
- removed the last stale reserved-config wording around `hash_space_bits` in Contract 28
- finalized README/docs/checklist/history surfaces so Phase 13 can start from a clean post-closeout baseline

Next:
- Phase 13a: observability expansion

PHASE_12_5F.txt
---------------
Phase 12.5f complete: Fjall note and doc closure.

Implemented:
- updated the Fjall storage/runtime design note to reflect the implemented Fjall-backed runtime path rather than a planning-only state
- removed stale module-path references such as `src/db/engine.rs` and `src/runtime/mod.rs`
- clarified that `replay-spool` remains filesystem/config based and does not open Fjall
- removed the stray dead doc artifact `docs/test_write.txt`
- aligned README/docs status surfaces so 12.5g is now the explicit next step

Next:
- Phase 12.5g: final consistency sweep and closeout

PHASE_12_5E.txt
---------------
Phase 12.5e complete: output sink and spool reconciliation.

Implemented:
- reconciled Contract 28 and Contract 29 with the actual active sink/runtime behavior
- locked spool helper controls as internal/helper behavior rather than active v0.1 config
- narrowed the sink contract so `replay-spool` remains the active filesystem/config replay surface without opening Fjall
- removed the stale helper-only `spool_replay_interval_s` field from `SpoolConfigV1` and updated sink tests

Next:
- Phase 12.5f: update the Fjall/runtime design note and finish the remaining doc-closure work

PHASE_12_5D.txt
---------------
Phase 12.5d - Observability contract narrowing

Implemented:
- narrowed Contract 10 to the current `sparx status`-centered observability surface
- removed stale v0.1 claims about Prometheus, health endpoints, and richer structured metrics streams
- reconciled Contract 28 metrics/health wording so the `[metrics]` section is explicitly deferred placeholder config only
- removed the last Prometheus-exporter implication from the tenant key-map contract

Notes:
- richer observability remains deferred to Phase 13
- no new runtime or endpoint code was added in this checkpoint

PHASE_12_5C.txt
---------------
Phase 12.5c complete: config contract reconciliation and validator hardening.

Implemented:
- classified the remaining Contract 28 config surface as active, reserved continuity, or deferred
- added validator coverage for active enum fields `sparx.log_level` and `sparx.log_format`
- kept reserved/deferred continuity fields parseable without promoting them to active runtime behavior
- added config tests for enum rejection and deterministic config-file/env/CLI precedence

Next:
- Phase 12.5d: narrow Contract 10 and related docs/config language to the current `status`-centered observability surface

PHASE_12_5B.txt
---------------
Phase 12.5b complete: hashed-fallback retirement across contracts and stale config surface.

Implemented:
- reconciled Contracts 04, 05, 25, and 28 with the locked dictionary-only FeatureId implementation
- retired the stale hybrid/hashed-fallback FeatureId contract language
- removed the stale `hash_fallback_enabled` config field from the active config structs, defaults, file/env loader, and tests
- kept `hash_space_bits` and `dict_gc_interval_s` documented as reserved compatibility/metadata fields pending the broader Config-contract reconciliation in Phase 12.5c

Next:
- Phase 12.5c: classify and validate the remaining config surface so Contract 28 matches actual runtime behavior

PHASE_12_5A.txt
---------------
Phase 12.5a complete: scope lock and checklist insertion for the post-Phase-12e closure work.

Implemented:
- opened Phase 12.5 as a bounded reconciliation/closure phase before Phase 13
- added Phase 12.5 to the working plan/checklist and current-status surfaces
- froze the closure targets from the Phase 12e review:
  - hashed-fallback drift
  - config validation gaps
  - loaded-but-unused config ambiguity
  - observability contract narrowing
  - spool contract/config mismatch
  - stale Fjall note wording
  - stray dead doc artifacts
- locked the decision to narrow observability contracts now and defer richer metrics/health endpoint work to Phase 13

Next:
- Phase 12.5b: retire stale hashed-fallback contract/config language without reviving hashed fallback behavior

PHASE_0A.txt
------------
Phase 0a scaffold checkpoint.

PHASE_0B.txt
------------
Phase 0b types-only skeleton checkpoint.

PHASE_1A.txt
------------
Phase 1a config loader + validation checkpoint.

PHASE_1B.txt
------------
Phase 1b CLI parsing + routing checkpoint.

PHASE_1C.txt
------------
Phase 1c status + config check checkpoint.
Fix3 removes the remaining build warnings reported by the user:
- remove unused i += 1 in src/cli/parse.rs
- remove unused env_i32 helper in src/config/load.rs

PHASE_2A.txt
------------
Phase 2a DB key builder checkpoint.
Implemented canonical global and tenant DB key builders from the locked prefix-map contracts,
including prefix helpers for deterministic prefix scans and tests that assert the expected paths.

PHASE_2B.txt
------------
Phase 2b tenant simple value encodings checkpoint.
Implemented deterministic tenant DB simple value encoders/decoders from the locked contract,
including little-endian fixed-width primitives, unsigned LEB128 varints, UTF-8 length-prefixed
strings, strict is_gzip validation, schema-version compatibility scaffolding, and contract-focused tests.

PHASE_2C.txt
------------
Phase 2c open-window checkpoint encoding checkpoint.
Implemented deterministic encoders/decoders for win_active, sparse feature counts,
window metadata, and top-K entity sketches from the locked contract, plus contract-
focused tests covering roundtrips, fixed sizes, deterministic ordering, and checkpoint
write/finalize sequencing using in-memory key simulations.

PHASE_2D.txt
------------
Phase 2d complete: baseline sketch encodings + tests.

Implemented:
- dfN fixed u32 encoding
- dfM varint pair encoding
- centroid varint+f32 pair encoding
- stats fixed 68-byte struct with always-present score_total Welford fields
- deterministic baseline encoding tests and rollover/sum checks

PHASE_3A.txt
------------
Phase 3a complete.

Implemented:
- stable hash lock for v0.1: BLAKE3, first 16 digest bytes, lowercase hex
- tenant/device directory discovery under watch_root
- deterministic device inventory ordering
- file discovery for regular files only
- hidden-file filtering
- no-symlink-by-default behavior with follow_symlinks support
- contract-default suffix allowlist: .log .txt .json .csv .cef .gz

Added:
- src/stable_hash.rs
- Phase 3a ingest discovery logic in src/ingest/mod.rs
- tests/ingest_discovery.rs

Next:
- Phase 3b cursor state machine per contract 31

PHASE_3B.txt
------------
Phase 3b complete:
- Added deterministic cursor state machine helpers for plain and gzip files.
- Locked reset behavior for inode change, truncation, gzip identity change, and type change.
- Added read-progress helper and contract-aligned tests.

Next planned subphase:
- Phase 3c reader abstraction (plain + gzip; gzip offsets are compressed-stream offsets).

PHASE_3C.txt
------------
Phase 3c complete:
- added streamed reader abstraction for plain and gzip files
- gzip reader tracks compressed-stream offsets and supports deterministic resume
- added tests for plain reads, gzip streaming/resume, caught-up gzip, invalid gzip, and zero chunk-size rejection

PHASE_3C_FIX1.txt
-----------------
Phase 3c fix1: corrected gzip reader offset progression and resume behavior.
- throttle compressed-source reads to keep offsets monotonic and resume-stable
- track logical next chunk offset separately from decoder internals
- normalize invalid gzip read errors to InvalidData/UnexpectedEof classes

PHASE_4A.txt
------------
Phase 4a checkpoint: syslog envelope split (BSD + ISO variants)

Implemented:
- Optional PRI peel
- RFC5424-like envelope parse with version, timestamp, host, app, procid, msgid, structured data
- RFC3164/BSD parse with month/day/time, host, tag, app/procid extraction, and year inference
- ISO timestamp heuristic parse with optional host and app extraction
- Bounded vendor-prefix peel for Cisco/ASA-like wrapped payloads
- Selected ts_guess according to envelope timestamp preference rules implemented so far
- Plaintext fallback with no hard failure

Added tests for:
- RFC5424 parse
- BSD parse and year inference
- ISO heuristic parse
- Cisco/ASA prefix peel
- Plaintext fallback
- PRI retention on fallback

Fix1: corrected vendor-prefix peeling to split on delimiter colons only, not timestamp-internal colons; normalized syslog timestamp tests to computed UTC expectations.

PHASE_4B.txt
------------
Phase 4b complete: generic tokenizer (KV/JSON/CSV + plaintext fallback).

Implemented:
- bounded bytes->string tokenizer entrypoint with lossy UTF-8 fallback and line cap
- JSON object flattening with deterministic key ordering, depth cap, and array scalar cap
- CSV row tokenization for file-level header mode
- generic KV scanner with quoted/bracketed/unquoted values
- plaintext word tokenizer with contract-aligned allowed characters
- deterministic token drop priority: structured pairs, residual words, quoted-value words

Added tests for:
- quoted KV values with spaces
- HTTP URL colon non-split behavior
- path and DOMAIN\\user word preservation
- JSON depth/array caps
- CSV header mode
- deterministic cap drop order
- plaintext fallback on JSON parse error
- bytes entrypoint lossy decode + length cap

Next: Phase 4c (CEF reverse parse).

Fix1:
- clamp lossy UTF-8 fallback output to the 16,384-byte line cap on char boundaries
- do not treat Windows drive-letter paths like C:\\... as colon KV pairs

PHASE_4C.txt
------------
Phase 4c completed: CEF reverse parse.

Changes:
- Added src/tokenize/cef.rs
  - Best-effort CEF header parsing by unescaped pipe delimiters.
  - Reverse extension KV parsing from the right.
  - Best-effort unescape support for \\, \|, \=, and \n.
  - Residual free-text capture for extension prefixes before the first parsed key.
- Updated src/tokenize/mod.rs
  - Exported parse_cef_message_v1.
- Updated src/tokenize/generic.rs
  - Added CEF precedence before JSON/CSV/generic KV.
  - Added cef_parse_errors_total_delta counter.
  - Emits CEF extension KVs first, then residual words, then low-priority CEF header events.
- Added tests/tokenize_cef.rs
  - reverse KV with spaces in values
  - escaped pipes and equals
  - parse failure fallback + counter
  - residual text word tokenization
- Updated contracts/23_tokenizer_details_v0_1.md
  - Named cef_parse_errors_total as the CEF parse error counter.

Next planned subphase:
- Phase 5a: feature dictionary (tenant-scoped feat_dict/v1/*)


Fix1:
- CEF fallback now returns plaintext words immediately on parse failure while incrementing cef_parse_errors_total.
- CEF value unescape now preserves literal backslashes unless escaping \\, \|, \=, or \n.

PHASE_5A.txt
------------
Phase 5a complete: tenant-scoped feature dictionary implemented.

Includes:
- in-memory feature dictionary state with deterministic forward/reverse maps
- persisted write generation for feat_dict/v1/str, feat_dict/v1/id, and meta keys
- strict validation when loading persisted forward/reverse maps
- explicit next-id seed on empty bootstrap to avoid assuming an uncontracted initial id
- tests for deterministic allocation, cap enforcement, and persisted consistency checks


Fix1:
- corrected feature dictionary persisted write key type to use db::keys::KeyBytes
- keeps generated dictionary writes aligned with the Phase 2a DB key builder contract

PHASE_5B.txt
------------
Phase 5b complete: feature emission per catalog.
- Added semantic key normalization/classification helpers.
- Added per-line feature emission for structured keys, syslog envelope fields, words, and plaintext IP shapes.
- Added user principal normalization (Option B) and identity metadata extraction.
- Added tests for k=/canon=/shape emission, metadata-only exact identities, and plaintext-only shape rules.

PHASE_5C.txt
------------
Phase 5c complete: entity sketch accumulator + deterministic top-K snapshot + checkpoint write generation for win_row ent/* with config cap conversion and tests.

PHASE_6A.txt
------------
Phase 6a complete: window accumulator + open-window checkpoint generation.
- Added deterministic UTC window alignment and weekday/weekend baseline bucket helpers.
- Added WindowAccumulatorV1 for one active per-device window with sparse counts, line/byte counters, and entity sketch state.
- Added restart-safe restore from win_active + win_row checkpoint data using the feature dictionary for family reconstruction.
- Added deterministic checkpoint write generation in the required order: feat, meta, ent/*, then win_active.
- Added cap-aware admission/drop handling for total features, words, plaintext shapes, and syslog features without inventing a hashed fallback namespace.
- Added atomic per-line apply semantics so dictionary insertion failures do not partially mutate window state.
- Added tests for alignment/buckets, apply-line behavior, cap drops, checkpoint restore, atomic failure behavior, and window key validation.

PHASE_6A_FIX1.txt
-----------------
Phase 6a fix1: restore-order bug fix.
- Adjusted WindowAccumulatorV1::from_checkpoint_v1 so missing FeatureId -> feature string mappings are reported before window-start alignment validation.
- This preserves restart validation of feature-family reconstruction from the tenant feature dictionary and matches the restore test expectation.
- No contract changes.

PHASE_6B.txt
------------
Phase 6b complete: deterministic window finalization planning.
- Added FinalizedWindowRowV1 as the immutable snapshot handed to later baseline/scoring phases.
- Added deterministic finalize plans with ordered mutations for two cases:
  - finalize idle window: delete feat/meta/ent keys, then delete win_active
  - finalize and advance: delete feat/meta/ent keys, then write the next win_active
- Added follow-on window creation with deterministic window_id increment and validation that the next window start is aligned and not earlier than the current exclusive end.
- Added tests for finalized row snapshots, idle finalize delete order, advance finalize active update order, invalid next-window starts, and window_id overflow.

PHASE_7A.txt
------------
Phase 7a complete: deterministic DF ring update planning.
- Added baseline::df_ring helpers for day_epoch/slot calculation and finalized-window DF updates.
- Added DfRingConfigV1, DfRingMetaStateV1, DfRingSlotBucketStateV1, DfRingMutationV1, and DfRingUpdatePlanV1.
- Implemented deterministic stale-slot clearing using explicit delete mutations for dfN/dfM keys discovered under the target slot prefixes.
- Implemented per-window presence updates for dfN and dfM using finalized sparse rows.
- Applied deterministic DF cap trimming by df_count descending with feature_id ascending tiebreak, then stored pairs in feature_id order for encoding.
- Added tests for UTC day/slot math, fresh updates, same-day accumulation, stale-slot rollover clearing, DF cap trimming, and invalid input rejection.
- last_roll_epoch remains untouched in 7a because the contracts define the key and encoding but do not define an update rule for it.

PHASE_7A_FIX1.txt
-----------------
Phase 7a fix 1

- Fixed saturating_add calls on mutable map entries in src/baseline/df_ring.rs for toolchain compatibility.
- No contract or behavior changes.

PHASE_7A_FIX2.txt
-----------------
Phase 7a fix2

Build compatibility fix for df_ring accumulation.
The BTreeMap entry default literal is now typed as 0u32 so saturating_add resolves
against the intended u32 counter type on older toolchains.

PHASE_7A_FIX3.txt
-----------------
Phase 7a fix3

Change:
- Renamed a local test variable in tests/baseline_df_ring.rs to avoid shadowing the row(...) helper function on some toolchains.

Reason:
- The prior test bound `let mut row = row(...)` and later called `row(...)` again in the same scope.
- That shadows the helper function name and causes a compile error.

Behavior:
- No production behavior changed. Test intent is unchanged.

PHASE_7A_FIX4.txt
-----------------
Phase 7a fix4

- Fixed stale-slot delete ordering in baseline df ring tests and implementation.
- Stale dfM/dfN delete keys are now normalized deterministically by:
  1) family order: dfM before dfN
  2) bucket order: numeric ascending bucket suffix
  3) raw byte fallback for exact tie stability
- This preserves duplicate removal and prefix validation.
- No contract behavior changed beyond making the deletion order match the intended deterministic ordering already asserted by tests.

PHASE_7A_FIX5.txt
-----------------
Phase 7a fix5: correct stale-slot delete ordering by parsing numeric bucket suffixes after the slot prefix slash.

PHASE_7B.txt
------------
Phase 7b: device centroid and stats update planning.

Added:
- baseline::centroid_stats module with deterministic weighted-row, centroid EMA,
  and device stats update helpers.
- Explicit planner config for centroid_alpha and centroid_cap so v0.1 does not
  guess an EMA alpha that is not locked in the contracts.
- Optional score_total update path so score stats remain empty until scoring is
  available in Phase 7c.
- Deterministic centroid cap handling: keep top absolute values, tie-break on
  feature_id ascending, then persist in feature_id order.
- Tests covering weighting, EMA updates, score_total optionality, zero-decay,
  config validation, missing feature strings, and stats counter overflow.

PHASE_7B_FIX1.txt
-----------------
Phase 7b fix1

Test-only compatibility fix.
- Renamed a local binding in tests/baseline_centroid_stats.rs to avoid shadowing the row(...) helper.
- No production behavior changed.

PHASE_7C.txt
------------
Phase 7c: scoring, alert object creation, deterministic reasons, and top features.

Added:
- alert scoring helpers implementing rarity, cosine drift, volume, label, and
  confidence rules from the scoring contract.
- deterministic top feature ranking, entity ordering, summary text, alert
  signature, alert_id, postcard encoding, and primary alert persistence bytes.
- tests covering rarity normalization, drift, cold start suppression,
  noise-suspect blob cases, deterministic ordering, postcard roundtrip, and
  reason code selection.

Important contract notes:
- Phase 7c only writes the primary alert object under alert/v1/<alert_id>.
  Secondary alert indexes are not added here because the current contracts do
  not define value encodings for alert_idx_* keys.
- Scoring uses the locked scoring math constants for info threshold, drift
  minimum, volume normalization, and cold-start minimum windows.
- Config fields scoring.cold_start_days and scoring.min_lines_per_window remain
  reserved because the current scoring contracts do not define how they should
  change emit or label behavior.

PHASE_7C_FIX1.txt
-----------------
Phase 7c fix1: correct centroid map helper types and cast rare-feature threshold to f64.

PHASE_8A.txt
------------
Phase 8a complete: JSONL + stdout output sinks.

Added:
- src/sink/mod.rs implementations for:
  - JsonlAlertSinkV1
  - StdoutAlertSinkV1
  - deterministic path and file-name helpers
  - UTC date mapping helpers
- tests/sink_output.rs

Behavior in 8a:
- JSONL sink writes one UTF-8 JSON object per line with trailing newline.
- Output path is deterministic:
  {alert_out_root}/tenant=<tenant_id>/device=<device_key>/YYYY/MM/DD/
- File names are deterministic:
  alerts_<device_key>_<YYYYMMDD>_<seq>.jsonl
- Rotation occurs on day change or after the current file size exceeds the configured rotate threshold.
- Existing seq collisions are handled deterministically by incrementing seq until a free file name is found.
- Stdout sink emits one JSON line per alert and flushes after each line.
- Unix directory/file modes are applied best-effort with cfg-gated no-op behavior on non-Unix platforms.

Not done in 8a:
- Spool write/replay/caps remain Phase 8b.
- No runtime wiring into the ingest/finalize path yet; this phase adds sink implementations and tests.

PHASE_8B.txt
------------
Phase 8b complete: spool write, replay, and cap enforcement helpers.

Added:
- src/sink/mod.rs spool support:
  - SpoolConfigV1
  - SpoolCountersV1
  - SpoolingJsonlAlertSinkV1
  - SpoolEmitOutcomeV1
  - SpoolReplayReportV1
  - SpoolCapReportV1
  - spool path/read/write/replay/cap helpers
- tests/sink_output.rs expanded with spool coverage

Behavior in 8b:
- On JSONL emit failure, alert is serialized to:
  {data_root}/spool/alerts/tenant=<tenant_id>/spool_<alert_id>.json
- Replay scans spool files deterministically by spool filename lex order.
- Successful replay flushes JSONL output and deletes the spool file.
- Failed replay leaves the spool file in place.
- Cap enforcement deletes deterministic oldest files by lex path order until total spool bytes are within cap.
- Local counters track spool writes, replay successes, replay failures, and cap drops.

Notes:
- This phase adds sink-level spool helpers and tests only.
- Global config schema still does not expose spool controls; that previously noted contract mismatch remains outside this subphase.

PHASE_9A.txt
------------
Phase 9a - validate-fixtures implementation

Implemented:
- fixture corpus validation helper module
- CLI routing for sparx validate-fixtures --fixture-root <path>
- deterministic traversal and stable validation output ordering
- validation of required fixture layout roots: tenants, golden, gen
- validation of tenant/device fixture files by supported extension
- shallow content checks for gzip, jsonl, csv, cef, golden json/jsonl, and gen json/toml
- tests for success, validation failure, and IO failure

Notes:
- This phase validates fixture corpus structure and sample readability only.
- It does not modify DB state.
- It does not add the full fixture corpus itself; that remains part of later smoke coverage work.

PHASE_9B.txt
------------
Phase 9b complete: E2E smoke + restart recovery.

Added:
- minimal repo fixture corpus under fixtures/
- smoke fixture golden/gen placeholders that validate under Phase 9a rules
- tests/e2e_smoke.rs to exercise:
  - fixture corpus validation
  - discovery
  - cursor planning and persisted cursor bytes
  - plain-file reader streaming
  - syslog peel + tokenizer + feature emission
  - window checkpoint persistence + restore
  - DF ring update
  - centroid/stats update
  - alert scoring
  - JSONL sink emission
  - restart recovery equivalence against single-pass processing

Notes:
- The restart smoke test preserves active-window provenance in the test harness snapshot so we can validate alert file spans end-to-end without inventing new tenant DB keys or changing locked v0.1 contracts.
- Production CLI run/oneshot wiring remains outside this phase.

PHASE_9B_FIX1.txt
-----------------
Phase 9b fix 1

- Fixed tests/e2e_smoke.rs to match WindowAccumulatorV1::apply_line_v1 byte_len type by converting u64 to usize with usize::try_from(...).unwrap().
- Removed unused imports in tests/e2e_smoke.rs.
- Removed unnecessary mut binding in restart smoke test.
- No production code or contracts changed.

PHASE_10A.txt
-------------
Phase 10a complete: CLI dispatch hardening + fail-closed behavior.

Added:
- main.rs now bypasses config load/validation for config-free commands:
  - version
  - validate-fixtures
- src/cli/route.rs now exposes command_requires_config_v1() and route_command_no_config_v1()
- unimplemented operational commands now fail closed with exit 1 instead of returning exit 0
- integration tests for config bypass and fail-closed behavior

Notes:
- This subphase does not add new operational command implementations.
- Status and config check still require config and continue to route through the config-backed path.
- Real DB/runtime foundation remains for later Phase 10 subphases.

PHASE_10B.txt
-------------
Phase 10b - Filesystem layout helper + canonical path derivation

Summary
- Added src/db/layout.rs as the authoritative runtime path derivation layer.
- Implemented FilesystemLayoutV1 from effective ConfigV1.
- Canonical helpers now derive:
  - data_root
  - tenant_root
  - global DB path
  - tenant DB root
  - alert output root
  - spool root
  - per-tenant DB directory
  - per-tenant alert directory
  - per-tenant spool directory
  - per-tenant policy path
- Updated config check routing to use db::layout instead of ad hoc path joins.
- Expanded config-check filesystem coverage to validate/create:
  - data_root
  - global_db_path
  - tenant_db_root
  - alert_out_root
  - spool_root
- Added dedicated db_layout tests for default paths, overridden paths, and deterministic tenant derivation.

Notes
- This subphase remains engine-neutral at the code boundary.
- No Fjall wiring is introduced yet.
- No runtime or repository logic is introduced yet.

Remaining work
- 10c Real global DB layer (Fjall)
- 10d Real tenant DB layer (Fjall)
- 10e Tenant DB handle cache + lifecycle
- 10f Runtime context + repository helpers

PHASE_10C.txt
-------------
Phase 10c completed the real Fjall-backed global DB layer.

Included in this checkpoint:
- fjall = 3.1.2 exact dependency pin
- thin Fjall raw KV wrapper in src/db/fjall.rs
- real global DB wrapper + repository helpers in src/db/global.rs
- raw get/put/delete/batch/prefix-scan/range-scan operations
- global schema/process/tenant/purge-journal/migrate-journal helpers
- integration tests using temp DB paths

No tenant DB wrapper or runtime cache was added in this subphase.

PHASE_10C_FIX1.txt
------------------
Phase 10c fix1

Reason
- user build against fjall 3.1.2 exposed API mismatches in src/db/fjall.rs

Fixes
- use closure form for Database::keyspace create options
- consume Guard values from prefix/range iterators via into_inner()
- pin fjall to =3.1.2 to avoid semver drift during local builds

Scope
- no contract changes
- no behavior changes beyond compile compatibility with fjall 3.1.2

PHASE_10D.txt
-------------
Phase 10d - Real tenant DB layer (Fjall)

Summary
- Added src/db/tenant.rs as the Fjall-backed tenant DB wrapper and repository helper layer.
- Added tenant DB open/create helpers using the canonical filesystem layout.
- Added deterministic raw get/put/delete/batch/prefix-scan/range-scan/persist passthroughs.
- Added tenant repository helpers for:
  - schema state read/write
  - cursor read/write
  - open-window checkpoint read/write/delete
  - DF slot/bucket read/write
  - centroid/stats baseline read/write
  - primary alert read/write/list
  - tenant migration journal append/scan
- Added tests/db_tenant.rs covering reopen persistence, alert roundtrip/listing, cursor/window/baseline roundtrips, and deterministic tenant migration journal scans.

Notes
- The tenant DB layer keeps Fjall-specific types inside src/db/.
- Primary alert access stays limited to alert/v1/<alert_id> objects, consistent with the locked 7c decision.
- Secondary alert indexes remain untouched.

Next
- Phase 10e - Tenant DB handle cache + lifecycle

PHASE_10E.txt
-------------
Phase 10e completed tenant DB handle cache + lifecycle for the Fjall-backed
runtime substrate. Added a deterministic cache with max-open cap, LRU eviction,
idle close, explicit close-for-purge, safe reopen, and coverage for cache
lifecycle behavior. Next: Phase 10f runtime context + repository helpers.

PHASE_10F.txt
-------------
Phase 10f completed the runtime context and repository helper layer.

Implemented in this checkpoint:
- src/runtime.rs
- runtime bootstrap of storage roots + global DB open
- runtime-owned access to config, layout, global DB, and tenant DB cache
- process metadata helper methods
- global repository helper passthroughs
- tenant cache / tenant DB access passthroughs
- tests/runtime_context.rs

Next: Phase 11a tenant purge implementation.

PHASE_11A.txt
-------------
Phase 11a - Tenant purge implementation

Implemented:
- real `tenant purge` route backed by `SparxRuntimeV1`
- tenant existence and status checks
- deterministic delete order: tenant DB, alerts, spool
- close cached tenant handle before deletion
- purge journal entries: requested, per-step deletes, complete on full success
- sets tenant status to terminated (3) and removes active index on full success
- partial delete behavior with exit 6 when some paths were deleted and others failed
- IO error behavior with exit 3 when no delete steps succeeded

Tests added:
- empty tenant success
- populated tenant success
- non-existent tenant
- rejected status unless --force
- partial delete failure
- all-delete-fail IO error
- cross-tenant safety

Next: Phase 11b migrate implementation.

PHASE_11B.txt
-------------
Phase 11b completed: migrate implementation.

What changed
- implemented real `sparx migrate --tenant <tenant_id>` and `sparx migrate --all` routing
- added runtime schema migration helpers for global and tenant DBs
- missing schema state now initializes to version 1
- older schema state upgrades in place to version 1
- tenant downgrade refusal now disables the tenant in the global DB and removes its active index entry
- deterministic migration journal writes added to global DB and tenant DB
- added tests for noop, init, downgrade refusal, deterministic --all ordering, and route exit codes

Notes
- current locked schema version remains 1 for both global DB and tenant DB in v0.1
- partial or invalid schema records continue to fail closed via existing read/decode errors

Next
- Phase 11c tenant policy show/check

PHASE_11B_FIX1.txt
------------------
Phase 11b fix1

Adjusted migrate downgrade-refusal route test for the locked single-owner Fjall runtime model.
The route test now drops the live runtime handle before reopening the same database path through CLI routing.

PHASE_11C.txt
-------------
Phase 11c completed: tenant policy show/check.

What changed
- added src/policy.rs for tenant policy TOML loading and validation
- implemented real routing for `sparx tenant policy show <tenant_id>` and `sparx tenant policy check <tenant_id>`
- policy loading now uses the contract-defined watch-root path under `<tenant_root>/<tenant_id>/.sparx/policy.toml`
- validation now checks:
  - `policy_version == 1`
  - deterministic `key_overrides` category names
  - optional `ip_bucket` CIDR syntax
  - default `min_identity_confidence = 2`
- added deterministic text output for `show` and summary output for `check`
- added tests for valid policy rendering, defaulting, invalid policy failures, and missing tenant/policy behavior

Notes
- policy show/check currently operate on the filesystem policy file only
- optional last-good policy caching in the tenant DB remains deferred because it is optional in the contract and not required for the CLI helper behavior
- this subphase does not invent additional policy semantics beyond the locked contract fields

Next
- Phase 11d alerts query/show/search/export

PHASE_11C_FIX1.txt
------------------
Phase 11c fix1

Fixes a Rust compile error in src/cli/route.rs where a newline character
was written as a literal line break inside a character literal in the tenant
policy error formatter. The code now uses an escaped '\n' character.

PHASE_11D.txt
-------------
Phase 11d complete.

Implemented alerts list/show/search/export on top of the Phase 10 Fjall-backed runtime/DB foundation.

Scope:
- alerts list supports time filtering and optional JSON output
- alerts show supports optional JSON output
- alerts search supports time filtering plus contains search over persisted AlertV1 fields
- alerts export writes deterministic JSONL and supports optional gzip
- commands scan primary alert objects and do not depend on secondary alert indexes

Key behavior:
- default ordering is window_start_ts descending, then alert_id ascending
- since is inclusive
- until is exclusive
- missing alert returns exit 1
- DB/runtime open failures return exit 4
- export IO failures return exit 3

PHASE_11D_FIX1.txt
------------------
Phase 11d fix1

Why the fix was needed
- alerts search matches across persisted alert text and entity fields.
- the original test searched for "alice", but all seeded sample alerts include user_ids = ["alice"], so the match set was broader than the test expected.

What changed
- updated tests/alerts_query.rs to search for "escalation" instead.
- this preserves the intended coverage for time-filtered text search while aligning the test with the implemented and documented search surface.

PHASE_11E.txt
-------------
Phase 11e complete.

Implemented:
- alert drill using AlertV1.provenance only
- alert extract using AlertV1.provenance only
- plain-file drill output with deterministic formatting
- gzip drill skip behavior with explicit reporting
- extract caps via --max-bytes and --max-lines
- filesystem resolution via <watch-root>/<tenant_id>/<device_path>/<file_rel>

Notes:
- drill skips gzip spans per contract
- extract streams plain and gzip spans through the existing reader abstraction
- commands return exit 1 for missing alert, 3 for IO failure, 4 for DB/runtime failure

Next:
- Phase 12a replay-spool command

PHASE_12A.txt
-------------
Phase 12a complete: replay-spool command.

Implemented:
- real `replay-spool [--tenant ...]` command path
- deterministic replay ordering by spool filename
- JSONL replay emits then flushes before deleting spool file
- partial failure returns exit 6 and preserves failed files
- command is filesystem/config based and does not open Fjall
- `output.sink=stdout` fails closed for replay

Tests added:
- replay all tenants
- replay single tenant
- replay success deletes files
- partial failure preserves failed files
- deterministic ordering
- stdout fail-closed

Remaining work:
- Phase 12b real status command
- Phase 12c oneshot mode
- Phase 12d run daemon mode
- Phase 12e tenant lifecycle runtime reconciliation

PHASE_12B.txt
-------------
Phase 12b complete: real status command backed by persisted/runtime state.

Implemented:
- real `status [--json]` command path using the runtime/global DB layer
- reports version, effective roots, window size, sink, tenant counts, and selected process/runtime state
- deterministic text and JSON output
- empty global DB reports zero counts and null process/runtime fields
- DB/runtime open or read failures return exit 4

Tests added/updated:
- empty global DB status text
- populated runtime state status JSON
- DB/runtime failure path
- existing config/no-config dispatch coverage retained

Remaining work:
- Phase 12c oneshot mode
- Phase 12d run daemon mode
- Phase 12e tenant lifecycle runtime reconciliation
- Phase 13a config contract reconciliation
- Phase 13b metrics/health MVP closure
- Phase 13c docs/contracts/release-readiness sync

PHASE_12B_FIX1.txt
------------------
Sparx Phase 12b fix1

Reason for fix
- status_json_populated_runtime_state_is_deterministic_v1 kept a live SparxRuntimeV1 handle open while invoking the status CLI route.
- Sparx uses a single-owner embedded DB model, so reopening the same live DB in the route is expected to fail.

Change made
- Updated tests/status_check.rs to drop the seeded runtime handle before calling route_command_v1 for status.

Why this is correct
- The status CLI runs as a separate open of the persisted global DB state.
- The test must release the original owner before exercising that path.
- No runtime or contract behavior changed in this fix.

Result
- Fixes the false-negative test failure without weakening the single-owner Fjall/runtime contract.

PHASE_12C.txt
-------------
Phase 12c completed oneshot mode.

Implemented:
- real oneshot parser/model with required --tenant and optional --since/--until/--device/--migrate
- real single-pass tenant-scoped processing through runtime + tenant DB
- deterministic device/file ordering
- cursor advancement and window finalization
- alert emission through configured sink
- partial device failure returns exit 6

Tests added:
- parser validation for required tenant and time range
- oneshot single tenant pass
- oneshot device filter
- oneshot time filter
- oneshot partial device failure

Remaining work:
- Phase 12d run daemon mode
- Phase 12e tenant lifecycle runtime reconciliation
- Phase 13 contract/config/docs/metrics hardening

PHASE_12C_FIX1.txt
------------------
sparx Phase 12c fix1

Reason:
- Fixed newline escaping regression introduced in src/cli/route.rs during 12c.
- Removed one unrelated unused_mut warning in src/drilldown.rs while touching the checkpoint.

Changes:
- Replaced invalid char literal newline with escaped '\n'.
- Replaced invalid byte literal newline with escaped b'\n'.
- Removed unnecessary mut from drilldown max_len local.

Behavior:
- No intended contract or runtime behavior change beyond restoring successful compilation.

PHASE_12C_FIX2.txt
------------------
Phase 12c fix2
- oneshot now processes plain files before gzip files per device
- partial-failure test uses the full smoke fixture on the successful device

PHASE_12C_FIX3.txt
------------------
Phase 12c fix3

Summary
- Adjusted oneshot integration tests to stop assuming the default scoring configuration will always emit alerts on a first-pass cold-start run.
- Preserved the core 12c runtime behavior and instead tightened the tests around deterministic cursor advancement and partial-failure handling.

What changed
- tests/oneshot_mode.rs
  - Removed the strict alert-output assertion from the single-tenant pass test.
  - In the partial-failure test, replaced the alert-output assertion with cursor advancement checks for the successful device.

Why
- The default config path used by the oneshot tests does not guarantee alert emission for the smoke fixture under cold-start scoring.
- The contract-critical behaviors for 12c are: deterministic processing, cursor advancement, device filtering, time filtering, and partial-success exit 6.
- These updated assertions validate those behaviors directly without inventing new scoring semantics.

PHASE_12C_FIX4.txt
------------------
Phase 12c fix4

Problem:
- oneshot gzip cursor assertions failed because the final saved cursor offset could stop at the last line boundary rather than the true compressed EOF offset.

Fix:
- after finishing per-file read in process_file_oneshot_v1, capture reader.current_source_offset_v1() and persist a final cursor write when it is ahead of the saved cursor.
- preserve last_read_ts; only advance offset to the true EOF position.

Impact:
- no contract change
- oneshot now leaves gzip cursors fully caught up to compressed file length after successful processing

PHASE_12D.txt
-------------
Phase 12d - Run daemon mode

Implemented in this checkpoint:
- real run route with `--migrate auto|off|require` parser/model support
- process-state start/end bookkeeping via global DB
- global schema handling at startup and per-tenant schema handling for active tenants
- deterministic tenant discovery and device processing order
- disabled tenants skipped without failing open
- clean shutdown path covering sink shutdown and final spool replay best-effort
- deterministic integration coverage using a test-only bounded-cycle env hook (`SPARX_TEST_RUN_MAX_CYCLES`)

Tests added:
- startup writes process state and flushes cursors/open-window state
- disabled tenant skipped
- second start after clean shutdown remains stable
- final spool replay best-effort

Next:
- Phase 12e tenant lifecycle runtime reconciliation

PHASE_12D_FIX1.txt
------------------
Phase 12d fix1

Summary
- Fixed run-daemon compile regressions in src/cli/route.rs.
- Fixed newline escaping in final spool replay warning formatting.
- Fixed ensure_run_signal_handler_v1() return type to Result<(), String>.
- Removed unreachable fallback match arm in route_command_v1().

Behavior
- No intended runtime contract change.
- This is a compile-fix checkpoint for 12d.

PHASE_12D_FIX2.txt
------------------
Phase 12d fix2

Adjusted cli_dispatch integration coverage now that run is implemented.
The old test still expected the pre-12d stubbed behavior for `run`.
The updated test now verifies fail-closed non-zero behavior for a real runtime discovery failure using an explicit missing tenant_root.

PHASE_12D_FIX4.txt
------------------
Phase 12d fix4

- Fixed malformed config string literal in tests/cli_dispatch.rs.
- No runtime behavior change.

PHASE_12D_FIX5.txt
------------------
Phase 12d fix5

- Fixed tests/cli_dispatch.rs to write Windows-safe TOML paths using literal strings.
- This prevents config load failures caused by unescaped backslashes in generated temp paths.

PHASE_12E.txt
-------------
Phase 12e - Tenant lifecycle runtime reconciliation

Implemented:
- daemon cycle now reconciles discovered tenants and known tenant records together
- disabled, terminating, and terminated tenants are skipped without restart
- skipped or undiscovered tenants are removed from the active index deterministically
- tenant last_seen_ts updates when tenant inventory is observed in a run cycle
- lifecycle transitions can be exercised in deterministic integration tests via a bounded-cycle test hook

Tests added:
- disable without restart
- terminating tenant no longer processes once marked
- active index reconciliation remains deterministic
- tenant last_seen_ts updates during run

Notes:
- no new CLI surface was added
- single-owner Fjall behavior remains unchanged
- this phase reconciles runtime behavior with existing tenant lifecycle contracts

PHASE_REBASE_2026_03_23.txt
---------------------------
Planning/documentation checkpoint after the Phase 9b review and the Phase 10a CLI hardening checkpoint.

Changes recorded in this checkpoint:
- storage engine target for the real DB/runtime layer set to Fjall
- `AlertV1.provenance: Vec<FileSpanV1>` confirmed as the only authoritative drilldown field model
- plan/checklist rebased so the DB/runtime foundation is its own Phase 10
- old operational extras shifted to Phase 11
- remaining runtime/service surfaces moved into Phase 12
- hardening/release-readiness work grouped into Phase 13
- contract wording updated to remove stale `source_files` / `device_dir` alert-field drift and to replace RocksDB-specific wording where the intent is an embedded DB

No Rust code changes are included in this checkpoint.
Next implementation subphase remains 10b.


PHASE_13A.txt
---------------
Phase 13a complete: observability expansion.

Implemented:
- activated Prometheus /metrics and health /healthz during run when enabled
- grounded endpoint output in real runtime/process/schema state and persisted run-cycle totals
- expanded status/status --json with the active observability surface
- activated metrics bind validation and fail-closed startup behavior

Next:
- Phase 13b: release hardening and final operator ergonomics

PHASE_15A - scoring policy activation
- activated scoring.cold_start_days as a day-based bucket maturity floor derived from window_size_s
- activated scoring.min_lines_per_window as a minimum alertable line-count floor while preserving baseline updates
- tightened config validation for active scoring thresholds and added scoring-policy tests

# Historical Roadmap Archive

This directory contains archived checkpoint notes with their original filenames
and wording. They are retained for project traceability and should not be treated
as the active user documentation set.

Use the active guides in the parent `docs/` directory for current user-facing
architecture, operations, alerting, configuration, validation, and release
readiness information.
